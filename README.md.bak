# n8n-cli

CLI Node.js (Commander) pour gérer des workflows n8n via API Key.

## Setup
```bash
cp .env.example .env
npm i
```

## Utilisation
```bash
# Liste les workflows
n8n-cli list

# Supprime un workflow par id
n8n-cli delete 123

# Supprime par nom (exact) ou par recherche
n8n-cli delete --name "My Workflow"
n8n-cli delete --search "WIP" --dry-run

# Export: un workflow (id) dans un fichier
n8n-cli export 123 -o ./exports/workflow-123.json

# Export: tous les workflows dans un dossier
n8n-cli export --all -o ./exports

# Import: crée ou update (si --update) depuis un fichier
n8n-cli import ./exports/workflow-123.json
n8n-cli import ./exports/workflow-123.json --update

# Import: batch d'un dossier
n8n-cli import ./exports --update

# Sync (bonus) : copie SRC -> DEST (par name, update sinon create)
```

- N8N_URL, N8N_API_KEY : profil "default"
- SRC_URL, SRC_API_KEY, DEST_URL, DEST_API_KEY : profil "sync"
